# Quantum extras, on top of requirements.txt.
#
# Install torchquantum WITHOUT its dependency list, which is stale and pulls in
# tensorflow, pyscf (no Windows wheels, needs a C++ toolchain) and qiskit<1.0.0,
# none of which statevector simulation uses:
#
#   pip install torchquantum --no-deps
#   pip install -r requirements-quantum.txt
#
# On Kaggle install these but never the pinned torch from requirements.txt.
# See quantum/README.md, and verify with: python -m quantum.smoke_test

opt_einsum
torchpack
torchdiffeq
tqdm
dill
pathos

# torchquantum 0.1.8 imports qiskit.providers.aer, which qiskit 1.x removed
qiskit==0.46.3
qiskit-aer==0.13.3
qiskit-ibm-runtime

# its qiskit code path still imports pkg_resources, dropped in setuptools 81
setuptools<81
